#include <stdio.h>
/**
 * main - Prog entry point
 * Return: Always return 0 (Success)
 */

int main(void)
{
	char b;

	b = 'z';
	while
		(b >= 'a')
		{
			putchar(b);
			b--;
		}
	putchar('\n');
	return (0);
}
